#!/usr/local/bin/python

import os
import random
import string

c = string.ascii_letters + string.digits
file_name = ''.join(random.choices(c, k=8))

with open(file_name, "w") as file:
    file.write("all your base are belong to us... --cats")

os.chmod(file_name, 0o400)