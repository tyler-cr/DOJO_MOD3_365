#!/usr/local/bin/python

import os
import random
import string

file_name = ''.join(random.choices(string.ascii_lowercase, k=8))

with open(f"/challenge/{file_name}", "w") as file:
    file.write("all your base are belong to us... --cats")

os.chmod(f"/challenge/{file_name}", 0o400)